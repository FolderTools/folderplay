{
  "name": "folderplay",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "node scripts/check.mjs",
    "deploy": "node scripts/check.mjs && astro build && node scripts/smoke.mjs && wrangler pages deploy dist --project-name=miusic --branch=main",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "acorn": "^8.17.0",
    "wrangler": "^4.0.0"
  }
}
